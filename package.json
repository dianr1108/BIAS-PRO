{
  "name": "bias-pro",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "vite build --config client/vite.config.ts",
    "dev": "vite --config client/vite.config.ts"
  }
}
